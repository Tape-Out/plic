name: plic
version: 0.1.0
spec: '0.1'
kind: ip
lang: bh
identity:
  slug: plic-tapeout-bsv
  display_name: Platform interrupt controller
  summary: PLIC and APLIC from one priority tree, selected by mode.
  category: peripheral
  subcategories:
  - interrupt
  ip_family: plic
  integration_profile:
  - apb4-slave
  maturity: planned
contract:
  version: 1
  ctrl:
    shape: flat
    aw: 24
    dw: 32
  irq:
  - name: eip
    kind: level
    width: contexts
params:
  sources:
    type: int
    default: 32
    range:
    - 1
    - 32
    desc: 中断源数。上限 32 是寄存器图定的——pending 与 enable 各只有一个 32 位字。 要撑到 PLIC 规范的 1023 源，得把这两个寄存器改成多字数组，
      那是软件可见的接口变更，不是实现里补一刀能了的。
  contexts:
    type: int
    default: 2
    range:
    - 1
    - 16
emit:
- kind: bsv
  package: Plic
  module: mkPlic
  config_type: PlicCfg
  interface: PlicIfc
  ctrl: regs
  pins:
  - name: pins
    type: PlicPins
    targs:
    - sources
- kind: verilog-flat
  bus: apb4
deps:
  hwcore: ^0.1
  amba: ^0.1
area:
  base:
    points:
      '4': 1634.36
      '8': 2252.88
      '16': 3468.64
      '32': 6028.96
    per: sources
  margin: 0.0241
  model: points-additive
  error:
    bound: 0.104
    sign: over
    note: parameter curves stored as measured points, features added on top, then
      lifted by 0.0241 so that no untested combination predicts under; the worst came
      in 1.91 percent under before the lift
  measured:
  - at:
      sources: 4
    um2: 1634.36
  - at:
      sources: 8
    um2: 2252.88
  - at:
      sources: 16
    um2: 3468.64
  - at:
      sources: 32
    um2: 6028.96
  corner:
    tool: ecc
    pdk: ics55
    freq_mhz: 100
    measured: '2026-09-09'
    gen_digest: sha256:a450df5a368f41dc
